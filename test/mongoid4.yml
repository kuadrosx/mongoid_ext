test:
  sessions:
    default:
      hosts:
        - localhost
      database: superlikers-test
      options:
        write:
          w: 0
  options:
    include_root_in_json: false
    include_type_for_serialization: true
    scope_overwrite_exception: true
    raise_not_found_error: true
    use_activesupport_time_zone: true
    use_utc: false
